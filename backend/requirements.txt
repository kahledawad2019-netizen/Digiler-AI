# Digiler AI — backend (thin API layer over the existing ala platform).
# Install the platform itself in editable mode from the repo root first:
#   pip install -e .
# then:  pip install -r backend/requirements.txt
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.6
email-validator>=2.1            # required by pydantic EmailStr (auth schemas)
pydantic-settings>=2.2
SQLAlchemy>=2.0
alembic>=1.13
asyncpg>=0.29            # PostgreSQL (production)
aiosqlite>=0.19          # SQLite (zero-infra local dev, default)
redis>=5.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<4.1            # passlib 1.7.4 reads bcrypt.__about__ (removed in bcrypt 4.1+)
python-multipart>=0.0.9
httpx>=0.27
sse-starlette>=2.1
