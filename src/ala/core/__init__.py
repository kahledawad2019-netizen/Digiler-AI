"""Core primitives shared across the platform.

Nothing in this subpackage depends on any other ALA subpackage — it is the
bottom of the dependency graph (enums, exceptions, hashing, ids, clock).
"""
